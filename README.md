# Loan-Default-System
Loan Default Prediction System — A full-stack ML web app built with Flask &amp; Python that predicts loan default risk using 4 algorithms (Logistic Regression, Random Forest, Decision Tree). Trained on 255K+ records with 88.52% accuracy. Features secure auth, prediction history, analytics dashboard &amp; cloud deployment.
